# CONCERNS — Technical Debt, Risks & Issues

> Auto-generated by GSD map-codebase | 2026-03-30

## 🔴 Critical Concerns

### 1. `bot_v5.py` is a God File (93KB / 1,514 lines)
- **Risk:** Extremely hard to maintain, debug, or extend
- **Impact:** Every bot change touches this massive file
- **Rule exists:** "Create separate module, don't modify core" — but file continues growing
- **Recommendation:** Extract handler groups into dedicated modules

### 2. No CI/CD Pipeline
- **Risk:** Deploy errors, untested code reaching production
- **Impact:** Manual deploy to VPS = human error risk
- **Current:** `git push` → SSH → `git pull` → build → restart
- **Recommendation:** GitHub Actions for test + deploy

### 3. Test Coverage ~15%
- **Risk:** Regressions go undetected
- **Impact:** Bot + ERP + WebApp + email = zero tests
- **6 test files** cover only data layer and 1 API route
- **Recommendation:** Priority test `bot_v5.py` handlers and API routers

## 🟡 Moderate Concerns

### 4. `email_engine/backup.pst` — 8GB in Repository
- **Risk:** Repo bloat, slow clone, not data that should be versioned
- **Impact:** Git performance degradation
- **Recommendation:** Move to `.gitignore`, store separately

### 5. Duplicate Data Access Patterns
- **Bot:** `query_engine.py` → Parquet via pandas/pyarrow
- **API:** `data_access.py` → Parquet via DuckDB
- **Risk:** Inconsistent query results between bot and API
- **Recommendation:** Unify under DuckDB DAL for all clients

### 6. Security Gaps
- **No authentication on most API routes** (only `auth_router.py` exists)
- **No rate limiting on API** (bot has `rate_limiter.py`, API doesn't)
- **`.env` files** not consistently gitignored
- **JWT:** `jose` library in webapp but middleware incomplete
- **GSD security hooks** now active (prompt guard, context monitor) — first layer added

### 7. Hardcoded VPS IP
- **`CLAUDE.md`** contains hardcoded IP: `14.225.207.145`
- **`deploy/` scripts** contain hardcoded IPs and paths
- **Recommendation:** Environment variables for all deployment targets

### 8. `__pycache__` Directories in Repo
- **Multiple `__pycache__/`** directories tracked
- **`.gitignore`** may not be complete
- **Recommendation:** Clean and add to `.gitignore`

## 🟢 Low Concerns (Good to Know)

### 9. ERP Excel/VBA Coupling
- **VBA macros** tightly coupled to Excel sheet structure (Row 1-8 layout)
- **Managed well** via locked functions + `FindSheet()` pattern
- **Risk:** Excel version upgrades could break VBA
- **Mitigation:** Already documented in `SYSTEM_RULES.md`

### 10. Multi-Machine Development
- **PC Home** (WebApp) + **Laptop VP** (Bot) + **VPS** (production)
- **Context sync:** `handoff.md` + CLAUDE.md
- **Risk:** Code divergence between machines
- **Mitigation:** GitHub as single repo + handoff protocol

### 11. Missing Formal Roadmap
- **Sprint planning** lives in `GEMINI.md` table — informal
- **No `ROADMAP.md`** with phased milestones
- **No `REQUIREMENTS.md`** with scoped deliverables
- **Being addressed:** GSD adoption adds formal planning artifacts

## Technical Debt Summary

| Priority | Item | Effort |
|----------|------|--------|
| 🔴 HIGH | Split `bot_v5.py` into modules | 2-3 sprints |
| 🔴 HIGH | Add CI/CD (GitHub Actions) | 1 sprint |
| 🔴 HIGH | Increase test coverage to 50%+ | 2-3 sprints |
| 🟡 MED | Remove 8GB `.pst` from git | 1 task |
| 🟡 MED | Unify data access (DuckDB everywhere) | 1 sprint |
| 🟡 MED | Complete API auth + rate limiting | 1 sprint |
| 🟡 MED | Environment-based deploy config | 0.5 sprint |
| 🟢 LOW | Clean `__pycache__` from repo | 1 task |
| 🟢 LOW | Formalize roadmap (GSD doing this) | 0.5 sprint |
