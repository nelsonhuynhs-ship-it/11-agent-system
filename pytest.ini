[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: integration test requiring Excel + xlwings
    slow: slow test (>5s)
addopts = -v --tb=short --strict-markers -p no:faulthandler
